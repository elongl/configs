# Disable Press & Hold
defaults write -g ApplePressAndHoldEnabled -bool false

# Keyboard Key Repeat
# Keyboard -> Key Repeat -> Fastest
# Keyboard -> Delay Until Repeat -> Shortest

# Enable Keyboard Navigation
# Keyboard -> Shortcuts -> Use keyboard navigation

# Do Not Disturb
# Keyboard -> Shortcuts - > Mission Control - Do Not Disturb -> ⌃⌥⌘D
# Define 10:00 -> 17:00 schedule

# Terminal
# Profiles -> Keyboard -> Use option as meta key

# Disable Smart Quotes
# System Preferences -> Keyboard -> Text -> Uncheck use smart quotes

# Bluetooth
# System Preferences -> Bluetooth -> Show Bluetooth in menu bar

# Don't show recent applications
# System Preferences -> Dock & Menu Bar -> Uncheck show recent applications in Dock

# Disable broken auto display management
# System Preferences -> Mission control -> Uncheck:
# - Automatically rearrange Spaces based on most recent use
# - When switching to an application, switch to a Space with open windows for the application
# - Group windows by application

# Disable True Tone (Makes external monitors yellow)
# System Preferences -> Display settings -> Uncheck true tone on MacBook's monitor

# Install
- Rectangle

